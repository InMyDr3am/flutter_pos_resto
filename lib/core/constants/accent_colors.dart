import 'package:flutter/material.dart';

/// A distinct accent color per owner feature, used on the dashboard and
/// other owner-facing surfaces so the UI doesn't read as a single flat
/// green-and-white palette.
class AppAccent {
  AppAccent._();

  static const menu = Color(0xFFEF6C00);
  static const ingredients = Color(0xFF00897B);
  static const purchases = Color(0xFF1E88E5);
  static const expenses = Color(0xFFD81B60);
  static const salesReport = Color(0xFF8E24AA);
  static const financialReport = Color(0xFF3949AB);
  static const employees = Color(0xFFF9A825);
  static const kasir = Color(0xFF2E7D32);
  static const kitchen = Color(0xFFE64A19);
}
